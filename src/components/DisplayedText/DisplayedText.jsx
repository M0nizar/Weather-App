function DisplayedText({ text }) {
  return (
    <div>
      <p style={{ font: "var( --text-preset4)", color: "var(--Neutral-0)" }}>
        {text}
      </p>
    </div>
  );
}

export default DisplayedText;
